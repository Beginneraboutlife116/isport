// import * as Form from '@radix-ui/react-form';

// type InputProps = {
// 	name: string;
// 	placeholder: string;
// 	type: string;
//   rule: Form.CustomMatcher;
// };

// export default function Input({ name, placeholder, type, rule }: InputProps) {
// 	return (
// 		<Form.Field name={name}>
// 			<Form.Control asChild>
// 				<input type={type ?? 'text'} required placeholder={placeholder} />
// 			</Form.Control>
// 			<Form.Message match={rule}>Email 不可為空</Form.Message>
// 		</Form.Field>
// 	);
// }
